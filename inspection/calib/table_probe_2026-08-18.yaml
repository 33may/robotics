# Table probe session 2026-08-18 — UR5e, TCP_normal [1.54, -6.72, 239.9] mm
# (pendant 4-point wizard, installation `inspection_demo`), closed RG2 tip,
# tool vertical, marks drawn by tape measure on the slab top.
#
# Anton measured in HIS frame: origin = corner next to the SINGLE post,
# coords given as depth/width. Conversion to the cell.yaml table frame
# (origin = front-left corner of slab top, x right, y away):
#   x = (52.9 - width) / 100        y = (100 - depth) / 100        z = 0
#
# raw readings (pendant, mm) — CAUTION: taken with Feature = "View", not
# "Base". View shared x/y with Base but its z was offset by a constant:
# re-touching P5 with Feature=Base read (100.98, -409.47, -5.43), so
# z_base = z_view + 399.47 mm. z values below are CORRECTED to Base.
#   P1 d90/w10 -> ( 259.8, -688.4, -404.7 view -> -5.23 base)
#   P2 d90/w46 -> (-104.9, -683.7, -404.5 view -> -5.03 base)
#   P3 d50/w40 -> ( -41.7, -285.6, -404.8 view -> -5.33 base)
#   P4 d50/w10 -> ( 255.6, -281.7, -405.0 view -> -5.53 base)
#   P5 d62/w26 -> ( 100.5, -409.5, -404.9 view -> -5.43 base)  # holdout
# LESSON: the Move tab displays Tool Position in the SELECTED FEATURE frame.
# Always set Feature = Base before recording probe points.

points:
  - name: P1
    table: [0.429, 0.100, 0.0]
    base: [0.2598, -0.6884, -0.00523]
  - name: P2
    table: [0.069, 0.100, 0.0]
    base: [-0.1049, -0.6837, -0.00503]
  - name: P3
    table: [0.129, 0.500, 0.0]
    base: [-0.0417, -0.2856, -0.00533]
  - name: P4
    table: [0.429, 0.500, 0.0]
    base: [0.2556, -0.2817, -0.00553]
  - name: P5
    table: [0.269, 0.380, 0.0]
    base: [0.1005, -0.4095, -0.00543]
    holdout: true
