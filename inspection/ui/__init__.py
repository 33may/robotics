"""Operator UI for the inspection loop.

`publisher.py` is the seam the loop calls; everything else here is the frontend
and the harness that lets it be developed without a robot. Nothing in this
package imports from `inspection.run`.
"""
