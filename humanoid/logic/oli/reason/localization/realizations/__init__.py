"""realizations/ — candidate localization algorithms, one folder each.

NEVER imported by brain code: candidates are selected by name and lazy-imported by their hosts
only (locbench / the localization host). Loop protocol + discipline: see AGENTS.md here.
"""
