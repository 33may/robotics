"""Depth estimation utilities for v016+ experiments.

Submodules:
    colorize          — depth-map colorization helpers (turbo, fixed-clip / per-frame-norm).
    add_gripper_depth — CLI: backfill a LeRobot v3.0 dataset with a `gripper_depth` video feature
                        produced by Depth Anything v2 metric.
"""
