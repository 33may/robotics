# August Memory

This folder is the August-side port of prior Claude memory for the VBTI/robotics project.

- `global/` contains the previous global Claude memory index.
- `project/` contains the previous robotics project memory files.
- `../knowledge/` contains generated Graphify/August codegraph artifacts for design-doc grounding.

Future project memory for this repo should be written here, not under `~/.claude/`.
